namespace Strikers.Components
{
    // Tags a scrolling-background tile. The tiles move down via Velocity like anything
    // else; BackgroundScrollSystem watches this tag and wraps a tile back to the top once
    // it has fully scrolled off the bottom, giving a seamless vertical scroll (PLAN.md §5).
    public class Background
    {
    }
}
